// Fieldmouse Economist checkpoint.
// URL construction can advance before authenticated transport does.

var command ← arguments(1);
var base ← "https://api.economist.com/eco/content/all/b2b-content-api";

if (command = "articles") {
  var from_date ← arguments(2);
  var to_date ← arguments(3);
  console.log(base + "/v1/articles?start_date=" + percentEncode(from_date) + "&end_date=" + percentEncode(to_date));
} else {
  if (command = "sections") {
    console.log(base + "/v1/sections");
  } else {
    if (command = "tags") {
      console.log(base + "/v1/tags");
    } else {
      if (command = "brief") {
        console.log(base + "/v1/the-world-in-brief");
      } else {
        // Live Economist requests require caller-supplied Content-Type and/or
        // Authorization headers. Keep current ICU limitation visible.
        console.log("SKIP\teconomist live: ICU custom request headers");
      }
    }
  }
}
