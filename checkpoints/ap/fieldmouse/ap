// Fieldmouse Associated Press checkpoint.
// Endpoint construction can advance independently of ICU header support.

var command ← arguments(1);
var operand ← arguments(2);
var base ← "https://api.ap.org/media/v";

if (command = "search-url") {
  console.log(base + "/content/search?q=" + percentEncode(operand) + "&page_size=25");
} else {
  if (command = "item") {
    console.log(base + "/content/" + operand);
  } else {
    if (command = "feed") {
      console.log(base + "/content/feed");
    } else {
      if (command = "rss") {
        console.log(base + "/content/rss");
      } else {
        if (command = "rss-id") {
          console.log(base + "/content/rss/" + operand);
        } else {
          if (command = "ondemand") {
            console.log(base + "/content/ondemand");
          } else {
            if (command = "live-choice") {
              console.log(base + "/content/live-choice-events");
            } else {
              if (command = "account") {
                console.log(base + "/account");
              } else {
                if (command = "followed-topics") {
                  console.log(base + "/account/followedtopics");
                } else {
                  if (command = "plans") {
                    console.log(base + "/account/plans");
                  } else {
                    if (command = "live-search") {
                      var key ← environment("AP_API_KEY");
                      console.log("SKIP\tap live-search: ICU custom request headers");
                    } else {
                      console.log("usage: ap {search-url QUERY | item ID | feed | rss | rss-id ID | ondemand | live-choice | account | followed-topics | plans | live-search QUERY}");
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
